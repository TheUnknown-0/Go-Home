package model;

/**
 * Enum representing the different types of fields on the board.
 */
public enum FieldType {
    START,      // Starting field for a player
    NORMAL,     // Regular field on the path
    HOME,       // Home field (goal area)
    FINISH      // Final winning position
}
